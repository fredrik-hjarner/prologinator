Here we try to load a uhm file, a file that has not
specified that it's a module to check if that does indeed
make that stuff in that file happen in user space.

That did not seem to be correct... no success when testing.